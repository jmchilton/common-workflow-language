inputs:
  - id: "#file1"
    depth: 0
    schema:
      type: File

requirements:
  - "@type": DockerContainer
    dockerRepo: arvados/jobs

baseCmd: [cat]

stdin:
  "@type": Transform
  language: jsonpointer
  value: "#inputs/file1/path"

stdout: output.txt
